using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MenuValues
{
    // Difficulty values are EASY == 0, MEDIUM == 1 & HARD == 2
    static public int m_Difficulty;

    // has a value between 0.1 & 1 (default value is 0.5)
    static public float m_MouseSensitivity = 0.5f;
}
